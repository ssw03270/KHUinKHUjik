<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khuinkhujik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".CheckTryActivity"></activity>
        <activity android:name=".ResultActivity" />
        <activity android:name=".Try2Activity" />
        <activity android:name=".TryRecordActivity" />
        <activity android:name=".TryInputActivity" />
        <activity android:name=".TryActivity" />
        <activity android:name=".JobActivity" />
        <activity android:name=".ShowJobActivity" />
        <activity android:name=".JobListActivity" />
        <activity android:name=".ShowJobListActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".SelectorActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-permission android:name="android.permission.DIAL_PHONE" />

</manifest>